import { StyleSheet } from 'react-native';
import Dimensions from 'Dimensions';
import Constants from '../../styles/Constants';

const CloudsStyle = StyleSheet.create({
  Clouds: {
    height: 200,
    width: 2000,
    position: 'absolute',
    resizeMode: 'contain',
    top: 100,
    left: 0
  }
});

export default CloudsStyle;